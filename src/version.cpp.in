#include "parakeet-audio/version.h"

auto parakeet_audio::get_version() -> const char* {
    return "@PROJECT_VERSION@";
}

auto parakeet_audio::get_full_version() -> const char* {
    return "@PROJECT_VERSION@ (git-@LIBPARAKEET_GIT_COMMIT_HASH@, @LIBPARAKEET_GIT_COMMIT_DATE@)";
}
